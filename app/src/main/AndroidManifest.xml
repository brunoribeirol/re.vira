<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Revira">

        <activity
            android:name="com.revira.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Adicione as novas atividades aqui -->
        <activity
            android:name="com.revira.ui.HomePageActivity"
            android:label="Home Page"
            android:exported="true">
        </activity>

        <activity
            android:name="com.revira.ui.PerfilActivity"
            android:label="Perfil"
            android:exported="true">
        </activity>

        <activity
            android:name="com.revira.ui.CadastrarEmpresaActivity"
            android:label="Cadastrar Empresa"
            android:exported="true"
            tools:ignore="MissingClass">
        </activity>

        <activity
            android:name="com.revira.ui.ProdutosRetornaveisActivity"
            android:label="Produtos Retornáveis"
            android:exported="true">
        </activity>

        <activity
            android:name="com.revira.ui.EditarProdutosActivity"
            android:label="Editar Produtos"
            android:exported="true">
        </activity>

        <activity
            android:name="com.revira.ui.PontosColetaActivity"
            android:label="Pontos de Coleta"
            android:exported="true">
        </activity>


    </application>

</manifest>
